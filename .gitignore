# Python
__pycache__/
*.py[cod]
*$py.class

# Virtual environment
.env
.venv
venv/
ENV/
env/

# IDEs and editors
.vscode/
.idea/
*.project
*.swp
*.swo

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Project-specific
# Large datasets - users should provide their own or download separately
data/*
!data/.gitkeep
!data/README.md
# ^ Allow a .gitkeep or README in data/ if you want to keep the dir structure

# Trained models - these can be very large
models/*
!models/.gitkeep
!models/README.md
# ^ Allow a .gitkeep or README in models/ if you want to keep the dir structure

# Training outputs (if any are consistently written to a specific dir)
trainer_output/

# Test artifacts (if tests create local temp files/dirs not in system temp)
*.test_artifacts/

# Notebook checkpoints
.ipynb_checkpoints

# Logs
*.log
logs/

# Temporary files
*.tmp
*.bak

# Specific files from our interaction (if not moved to tests/ or part of examples)
# test_ppo_fix.py (if considered a dev/test script not for general users, otherwise keep)
# test_ppo_model.py (same as above)

# Accelerate and other training cache/config
.accelerate/ 